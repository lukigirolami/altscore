services:
  - type: web
    name: phase-change-api
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
